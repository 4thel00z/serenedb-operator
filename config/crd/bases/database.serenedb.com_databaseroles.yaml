---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: databaseroles.database.serenedb.com
spec:
  group: database.serenedb.com
  names:
    kind: DatabaseRole
    listKind: DatabaseRoleList
    plural: databaseroles
    singular: databaserole
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cluster.name
      name: Cluster
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DatabaseRole is a role created inside a SereneDB server.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseRoleSpec defines a role inside a SereneDB server.
              Roles are global to the server.
            properties:
              cluster:
                description: Cluster is the SereneDB that hosts the role.
                properties:
                  name:
                    description: Name of the SereneDB.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              connectionLimit:
                description: ConnectionLimit is stored by the server for compatibility
                  and not enforced. Nil leaves it unset.
                format: int32
                type: integer
              createDB:
                description: CreateDB lets the role create databases.
                type: boolean
              createRole:
                description: CreateRole lets the role create, alter and drop other
                  roles.
                type: boolean
              inRoles:
                description: InRoles lists roles this role is a member of. Memberships
                  not listed here are revoked.
                items:
                  type: string
                type: array
              inherit:
                default: true
                description: Inherit makes the role use privileges of roles it is
                  a member of.
                type: boolean
              login:
                description: Login lets the role start client connections.
                type: boolean
              name:
                description: Name of the role on the server. Defaults to the object
                  name.
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              passwordSecret:
                description: PasswordSecret holds the password. It is applied on creation
                  and again whenever the Secret changes.
                properties:
                  key:
                    default: password
                    description: Key inside the Secret.
                    type: string
                  name:
                    description: Name of the Secret in the same namespace.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              reclaimPolicy:
                default: Retain
                description: ReclaimPolicy decides whether the role is dropped when
                  this object is deleted.
                enum:
                - Retain
                - Delete
                type: string
              superuser:
                description: Superuser bypasses every permission check.
                type: boolean
              validUntil:
                description: ValidUntil is the timestamp after which the password
                  stops working, in any format the server accepts.
                type: string
            required:
            - cluster
            type: object
          status:
            description: DatabaseRoleStatus defines the observed state of DatabaseRole.
            properties:
              conditions:
                description: Conditions hold Ready.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the spec generation the status
                  reflects.
                format: int64
                type: integer
              passwordSecretVersion:
                description: PasswordSecretVersion is the resourceVersion of the password
                  Secret last applied to the role.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
