---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serversecrets.database.serenedb.com
spec:
  group: database.serenedb.com
  names:
    kind: ServerSecret
    listKind: ServerSecretList
    plural: serversecrets
    singular: serversecret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cluster.name
      name: Cluster
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServerSecret is a persistent secret in a SereneDB server's secrets
          manager.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ServerSecretSpec defines a persistent secret in the SereneDB secrets manager, the credentials
              the server uses to reach object storage, HTTP endpoints, Iceberg catalogs and remote databases.
              The server stores persistent secrets unencrypted on its data volume.
            properties:
              cluster:
                description: Cluster is the SereneDB that holds the secret.
                properties:
                  name:
                    description: Name of the SereneDB.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              name:
                description: Name of the secret on the server. Defaults to the object
                  name.
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              options:
                additionalProperties:
                  type: string
                description: Options are non-sensitive CREATE SECRET options such
                  as REGION or ENDPOINT, keyed by option name.
                type: object
              scope:
                description: Scope is a path prefix the secret applies to, for example
                  s3://bucket/. Empty applies to every path of the type.
                type: string
              type:
                description: Type selects the service the secret is for.
                enum:
                - azure
                - gcs
                - http
                - huggingface
                - iceberg
                - postgres
                - r2
                - s3
                type: string
              valuesFrom:
                description: ValuesFrom names a Kubernetes Secret whose every key
                  becomes a CREATE SECRET option, such as KEY_ID and SECRET.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - cluster
            - type
            type: object
          status:
            description: ServerSecretStatus defines the observed state of ServerSecret.
            properties:
              conditions:
                description: Conditions hold Ready.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the spec generation the status
                  reflects.
                format: int64
                type: integer
              valuesSecretVersion:
                description: ValuesSecretVersion is the resourceVersion of the Kubernetes
                  Secret last applied to the server.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
