---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: scheduledbackups.database.serenedb.com
spec:
  group: database.serenedb.com
  names:
    kind: ScheduledBackup
    listKind: ScheduledBackupList
    plural: scheduledbackups
    singular: scheduledbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cluster.name
      name: Cluster
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .spec.suspend
      name: Suspended
      type: boolean
    - jsonPath: .status.lastScheduleTime
      name: Last
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScheduledBackup creates Backups of a SereneDB on a schedule.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduledBackupSpec creates Backups of a SereneDB on a cron
              schedule.
            properties:
              cluster:
                description: Cluster is the SereneDB to back up.
                properties:
                  name:
                    description: Name of the SereneDB.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              immediate:
                description: Immediate creates one Backup right after the schedule
                  is created.
                type: boolean
              keep:
                description: Keep is how many completed Backups from this schedule
                  to retain. Older ones are deleted. Nil keeps all.
                format: int32
                minimum: 1
                type: integer
              method:
                default: volumeSnapshot
                description: Method is copied into each Backup.
                enum:
                - volumeSnapshot
                type: string
              schedule:
                description: Schedule is a five-field cron expression evaluated in
                  UTC, for example "0 2 * * *".
                minLength: 9
                type: string
              suspend:
                description: Suspend stops new Backups from being created without
                  deleting the schedule.
                type: boolean
              volumeSnapshotClassName:
                description: VolumeSnapshotClassName is copied into each Backup.
                type: string
            required:
            - cluster
            - schedule
            type: object
          status:
            description: ScheduledBackupStatus defines the observed state of ScheduledBackup.
            properties:
              conditions:
                description: Conditions hold Ready, which is false when the schedule
                  cannot be parsed.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastBackup:
                description: LastBackup names the most recent Backup created by this
                  schedule.
                type: string
              lastScheduleTime:
                description: LastScheduleTime is when the most recent Backup was created.
                format: date-time
                type: string
              nextScheduleTime:
                description: NextScheduleTime is when the next Backup is due.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
